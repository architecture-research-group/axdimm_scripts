use run.sh script to test
./run.sh

copies single char (2 bytes) to memory at offset 256
-- character "c" is written to the memory location

to perform char copy test:
sudo ./run.sh test=2

to perform recognizable data copy test (128 1's 128 0's 128 alternating):
sudo ./run.sh test=1
