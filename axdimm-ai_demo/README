use install.sh script to test
./install.sh

copies single char (2 bytes) to memory at specified offset parameter
-- character "c" is written to the memory location

to perform char copy test: (copy an 'A' to offset 256
sudo ./install.sh test=0 offset=256 char=65

to perform recognizable data copy test (128 1's 128 0's 128 alternating):
sudo ./install.sh test=1
